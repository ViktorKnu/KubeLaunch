# CLI

This directory will contain the Python/Typer `kube-launch` CLI beginning in
Milestone 1. Its responsibility is limited to local cluster lifecycle, Argo CD
bootstrap, and reporting platform status.
